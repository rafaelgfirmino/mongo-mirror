config:
  source:
    connectionString:
    database:
  destiny:
    connectionString:
    database:
  timeout: //default 60 seconds
  tenants:
    -
collections:
  - name:
    batchSize: "all"
    upsert:
    multiTenant:
    filter: |
      {
      
      }